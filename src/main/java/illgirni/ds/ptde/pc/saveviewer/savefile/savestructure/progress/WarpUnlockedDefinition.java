package illgirni.ds.ptde.pc.saveviewer.savefile.savestructure.progress;

// only needed when we want to switch to the bit that enables the warp menu in game
// and not the quest indicator
public class WarpUnlockedDefinition extends AbstractBitDefinition<Enum<?>> {

  public WarpUnlockedDefinition(int bitOffset) {
    super(null, bitOffset);
  }

}
